node_modules/
dist/
build/
.env
.env.local
.env.production
*.log
.DS_Store
Thumbs.db
.pnpm-store/
apps/server/prisma/migrations/
# Firebase — injected from GitHub Secret during CI/CD, never commit
apps/android/app/google-services.json
# Release keystore — injected from GitHub Secret during CI/CD, never commit
sleek-release.keystore

# Signing credentials � never commit
keystore.properties
*.keystore
*.jks
