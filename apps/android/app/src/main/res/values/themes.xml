<resources>
    <!-- For a pure Compose app the XML theme only shows for a split second.
         Use the Android framework theme — always available, no extra dependency. -->
    <style name="Theme.Sleek" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.Sleek.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.Sleek</item>
    </style>
</resources>
